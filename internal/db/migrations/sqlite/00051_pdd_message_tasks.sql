-- +goose Up
CREATE TABLE pdd_message_tasks (id TEXT PRIMARY KEY,user_id INTEGER NOT NULL,idempotency_key TEXT NOT NULL,pdd_account_id TEXT NOT NULL,goods_id TEXT NOT NULL DEFAULT '',sku_id TEXT NOT NULL DEFAULT '',mall_sn TEXT NOT NULL DEFAULT '',captured_chat_url TEXT NOT NULL DEFAULT '',task_type TEXT NOT NULL,message_text TEXT NOT NULL,message_fingerprint TEXT NOT NULL,business_id TEXT NOT NULL DEFAULT '',xianyu_order_id TEXT NOT NULL DEFAULT '',pdd_order_id TEXT NOT NULL DEFAULT '',metadata_json TEXT NOT NULL DEFAULT '{}',source_platform TEXT NOT NULL DEFAULT '',source_conversation_id TEXT NOT NULL DEFAULT '',source_message_id TEXT NOT NULL DEFAULT '',parent_task_id TEXT NOT NULL DEFAULT '',reply_expected INTEGER NOT NULL DEFAULT 0,send_mode TEXT NOT NULL DEFAULT 'manual_confirm',priority INTEGER NOT NULL DEFAULT 0,scheduled_at INTEGER NOT NULL DEFAULT 0,status TEXT NOT NULL DEFAULT 'pending',worker_id TEXT NOT NULL DEFAULT '',lease_token TEXT NOT NULL DEFAULT '',lease_expires_at INTEGER NOT NULL DEFAULT 0,attempts INTEGER NOT NULL DEFAULT 0,sent_at INTEGER NOT NULL DEFAULT 0,verified_at INTEGER NOT NULL DEFAULT 0,last_error TEXT NOT NULL DEFAULT '',result_json TEXT NOT NULL DEFAULT '{}',created_at INTEGER NOT NULL,updated_at INTEGER NOT NULL,FOREIGN KEY(user_id) REFERENCES users(id),UNIQUE(user_id,idempotency_key));
CREATE INDEX idx_pdd_message_tasks_queue ON pdd_message_tasks(user_id,status,scheduled_at,priority,created_at);
CREATE TABLE pdd_message_merchant_locks (user_id INTEGER NOT NULL,pdd_account_id TEXT NOT NULL,mall_sn TEXT NOT NULL,task_id TEXT NOT NULL,expires_at INTEGER NOT NULL,PRIMARY KEY(user_id,pdd_account_id,mall_sn));
-- +goose Down
DROP TABLE IF EXISTS pdd_message_merchant_locks;
DROP TABLE IF EXISTS pdd_message_tasks;
